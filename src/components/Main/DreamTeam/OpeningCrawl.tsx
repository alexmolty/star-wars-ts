import {textDreamTeam} from "../../../utils/constants.ts";

const OpeningCrawl = () => {
        return (
            <p className="text-[1.7em] text-justify leading-[1.6]">{textDreamTeam}</p>
        );
    }

export default OpeningCrawl;